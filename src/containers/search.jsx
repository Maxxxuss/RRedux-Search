// import { connect } from 'react-redux';
// import { initializeResources, receiveResult, indexResource, search } from '../redux/actions/search';
// import ReduxSearch from '../components/search/reduxSearch';

// // import Selectors

// // export default connect(
// //     mapStateToProps,
// //     mapDispatchToProps,
// // )(Notepad);

// const mapDispatchToProps = (dispatch) => ({
//     initializeResources: (resourceNames) => dispatch (initializeResources(resourceNames)),
//     receiveResult: (resourceName) => dispatch (receiveResult(resourceName)),
//     indexResource: (resourceName) => dispatch (indexResource({
//         fieldNamesOrIndexFunction: resourceIndex,
//         resourceName,
//         resources: resource,
//         state: nextState
//      })),
//      search: (resourceName) => dispatch (search(resourceName)),


// })

// export default connect(
//     null, 
//     mapDispatchToProps,
// )(ReduxSearch)